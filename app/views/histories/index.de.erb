<%#
# Shows the Action-History of an Event
%>

<h1>Aktivitäten</h1>

<p>
  Hier finden Sie die Aktivitäten der letzten 30 Tage
</p>

<p>
  <%= start_roundedBox(600) %>

    <% @histories.each do |hist| %>    
    <div class="message">
      <div class="msg_date">          
          <%= german_time_format(hist.created_at) %>
      </div>
      <%= h(hist.action) %>
    </div>
    <% end %>

  <%= end_roundedBox(600) %>
</p>
